.env
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
# Только рабочий каталог в корне: база, голосовые, кэш синтеза, модели.
# Без ведущего слэша правило совпадало бы и с english_bot/content/data/ —
# то есть выбрасывало бы из репозитория весь курс.
/data/
.venv/
